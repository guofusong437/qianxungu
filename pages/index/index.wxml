<view class="container">
    <logoComponent></logoComponent>
    <CalendarComponent bind:showMaskView="showMaskView"></CalendarComponent>
    <MaskComponent wx:if="{{isShowMask}}"></MaskComponent>
</view>

